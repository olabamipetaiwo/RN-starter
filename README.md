## Expo react native starter app with nativwind

### `npm install` or `yarn install`


### `npm start` or `yarn start`

#### `npm run ios` or `yarn run ios`

#### `npm run android` or `yarn run android`.

